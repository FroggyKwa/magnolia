from django.urls import path
from search import views

urlpatterns = [
    # path("building/", views.BuildingViewSet.as_view(), name="building"),
    # path("department/", views.DepartmentViewSet.as_view(), name="department"),
    # path("teacher/", views.TeacherViewSet.as_view(), name="teacher"),
]